import React from "react"
import ReactDOM from "react-dom/client"
import SolarCalc from "./SolarIQ"
ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode><SolarCalc /></React.StrictMode>,
)
